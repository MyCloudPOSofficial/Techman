{
  "name": "cloud-pos-backend",
  "version": "1.0.0",
  "description": "Real Express + PostgreSQL backend for the Cloud POS app (ported from the app's embedded simulated backend).",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "migrate": "node db/migrate.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "pg": "^8.12.0",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
